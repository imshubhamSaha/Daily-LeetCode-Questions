
// User function Template for javascript
class Solution {
    rotateDelete(arr) {
        const n = arr.length;
        return n == 1 ? arr[0] : (n % 2 == 0 ? arr[2 + Math.floor((n - 2) / 4) - 1] : arr[3 + Math.floor((n - 3) / 4) - 1]);
    }
}
