class Solution {
    findSmallest(arr) {
        let ans = 1;
        
        for (const num of arr) {
            if (ans >= num) {
                ans += num;
            }
        }
        
        return ans;
    }
}
