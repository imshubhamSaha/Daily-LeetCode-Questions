class Solution {
    // arr: input array
    // n: size of array
    //Function to sort the array into a wave-like array.
    convertToWave(n, arr)
    {
        for(let i=0; i<n-1; i+=2) {
            [arr[i] , arr[i+1]] = [arr[i+1], arr[i]];
        }
        
        return arr;
    }
}
